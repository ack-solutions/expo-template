This folder is generated/managed by `drizzle-kit`.

- Run `pnpm db:generate` after updating `src/database/schema/*`.
- The generated `migrations` bundle is imported at runtime to run migrations on device startup.

